struct Point {
    x: int,
    y: int
}

struct Rectangle {
    topLeft: Point,
    width: int
}

let rect: Rectangle = Rectangle {
    topLeft: Point { x: 0, y: 0 },
    width: 100
};

print(rect.topLeft.x);
